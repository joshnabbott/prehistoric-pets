<h1>New product</h1>

<% form_for([:admin, @product]) do |f| %>
  <fieldset>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <p>
      <%= f.submit "Create" %> - or - <%= link_to 'Cancel', admin_products_path %>
    </p>
  </fieldset>
<% end %>

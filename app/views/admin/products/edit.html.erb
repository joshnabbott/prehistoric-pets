<h1>Editing product</h1>

<% form_for([:admin, @product], :html => { :multipart => true }) do |f| %>
  <fieldset>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <p>
      <%= f.submit "Update", :class => 'submit' %> - or - <%= link_to 'Cancel', admin_products_path %>
    </p>
  </fieldset>
<% end %>
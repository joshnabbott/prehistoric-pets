<h1>New category</h1>

<% form_for([:admin, @category]) do |f| %>
  <fieldset>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <p>
      <%= f.submit "Create", :class => 'submit' %> - or - <%= link_to 'Cancel', admin_categories_path %>
    </p>
  </fieldset>
<% end %>
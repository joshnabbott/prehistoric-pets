<h1>New post_o_matic_posting</h1>

<% form_for([:admin, @post_o_matic_posting]) do |f| %>
  <fieldset>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <p>
      <%= f.submit "Create" %> - or - <%= link_to 'Back', admin_post_o_matic_postings_path %>
    </p>
  </fieldset>
<% end %>